# PISTOL PANIC → PHASER: единый документ миграции

Документ для агента, который переносит игру с Unity на Phaser. Содержит всё:
вердикт, обоснование, полную ГДД-выжимку с каноничными решениями владельца,
архитектуру целевого проекта, карту переноса, интеграцию Yandex Games SDK,
план фаз, правила кода и верификацию. Самодостаточен: читай его одним файлом,
затем — PHASES_README.txt и AI_CONTEXT.md в этом репо как справочники.

---

## 0. ВЕРДИКТ

**Переходим на Phaser 3 + TypeScript + Vite. Unity-версия заморожена.**

Почему (сверка с амбициями владельца, все они задокументированы в этом репо):

| Амбиция (из документов проекта) | Unity WebGL | Phaser |
|---|---|---|
| Эксклюзивно Yandex Games (веб) | Поддерживается, но это самый тяжёлый путь Unity | Родная среда: ЯИ — это HTML5-игры |
| Слабые мобилы без лагов (прямое требование владельца) | 20–40 МБ wasm, компиляция при загрузке, память | 1–3 МБ, мгновенный старт |
| Скорость итераций (один владелец + агенты) | Редактор, домен-релоады, YAML/meta/гайды — весь этот болезненный опыт уже был | Только текст и `tsc`, вся работа в git, CI тривиален |
| Live-ops: реклама, сейвы, лидерборды, локализация, платежи ЯИ | PluginYG2 (тяжёлый, хрупкая интеграция с редактором) | `@yandex-games/sdk-dev`: тонкие JS-вызовы |
| Игра: 2D тап-шутер с ручной математикой без физдвижка | Избыточно (излишне — подозрение владельца верное) | Ровно то, что нужно |

Unity остаётся в репо как замороженный референс (ветки
`otecGlinomes83/PistolPanic` = рабочий коммит `cc38ddc`, проверен компиляцией
и запуском; ветка `unity-pixelart-wip` = недоведённый процедурный пиксель-арт,
использовать как арт-референс). Новые фичи в Unity НЕ делать.

Единственная причина остаться на Unity — если владелец планирует Steam /
нативные сторы / 3D / не-веб проекты. В документах этого нет: везде
«WebGL → Yandex Games». Если планы изменились — остановись и спроси.

---

## 1. ИГРА В ОДИН АБЗАЦ (концепция)

Pistol Panic — портретный 2D-дуэльный тап-шутер для Yandex Games.
Две «пушки» на арене, у каждой ствол вращается от физики (спавн-импульс,
отдача, торк, отскоки от стен и препятствий). Игрок **не целится никогда** —
управляет только выстрелами (решение владельца, канон). Пули летят по
прямой; вражеские и свои пули при встрече взрываются. Враг — ИИ со зрением.
Победа выбивает деньги и уровень; между боями — магазин «АРСЕНАЛ», где
меняется оружие. Три стиля стрельбы: одиночный (пистолет), очередь
(автомат), веер дроби (дробовик). Дальше по роадмапу: парирование,
баффы, перки, скины, реклама, лидерборды, локализация ru/en/tr.

---

## 2. КАНОН РЕШЕНИЙ ВЛАДЕЛЬЦА (не подлежит обсуждению при переносе)

1. Игрок НЕ целится. Направление ствола меняется только физикой.
   `aim`-вход не добавлять никогда.
2. Взрыв пуля×пуля домажит **того, кто в радиусе** (оба в радиусе — оба).
   Радиус поражения учитывает радиус пушки.
3. ИИ: смена стадии врага НЕ сбрасывает визибилити-гистерезис; старт боя
   не считается «появлением» игрока (первый быстрый выстрел был багом).
4. Поражение ведёт в магазин (как победа), без денег за поражение.
5. Баланс — в конфигах, ассеты = живая правда (владелец крутит в тестах).
   Ни одно число из документации не «чинит» конфиг.
6. Стиль решений: «как можно проще, но со вкусом», приоритет — слабые
   мобилы. Никаких спекулятивных абстракций.
7. Лимит на саб-агентов в одной задаче: 20–30.
8. Поведенческие изменения — только через явный вопрос владельцу.
   Структура/стиль — без вопросов.
9. Комментарии в коде запрещены. Код — английский, UI-тексты — русские
   (локализация ru/en/tr появится в М6).

---

## 3. ГДД (полная выжимка; оригинал у владельца — Desktop/GDD_pistol_panic_v2.2.txt,
при переносе вставить сюда разделами, канон чисел ниже)

### 3.1 Поток игры
`Bootstrap (ждёт YG SDK) → Дуэль: Armed → [тап] Grace (1с) → Fight →
Victory / Defeat → оверлей результата → Shop (АРСЕНАЛ + В БОЙ) → новая
дуэль`. Уровень растёт на 1 за КАЖДЫЙ исход матча (и за победу, и за
поражение). Тип врага = `(уровень − 1) % 3`: уровень 1 — пистолетчик
(красный), 2 — автоматчик (оранжевый), 3 — дробовик (фиолетовый), далее
по кругу.

### 3.2 Арена и физика пушек
- Прямоугольная арена 5.625 × 10 юнитов, портрет 9:16, ортокамера = арена
  ровно по высоте. Пушки: круги радиуса 0.4.
- Спавн по противоположным точкам (0, −4.2) и (0, +4.2), повороты 90°/270°
  друг на друга; случайный импульс 1.5–3.0 в случайном направлении и
  случайная угловая скорость −120..120 °/с.
- Каждый тик (фиксированный 1/60 с): линейный дампинг ×(1 − 0.4·dt),
  угловой ×(1 − 1.2·dt); стоп при скорости < 0.05 (угловой < 5°/с);
  интегрирование позиции; отскоки от стен (отражение компоненты);
  выталкивание из круговых препятствий (радиус 0.5, отражение нормальной
  компоненты скорости); расталкивание пушек пополам с обменом нормальных
  скоростей при сближении.
- Отдача: `velocity −= направление · RecoilImpulse`; торк
  `±Random(RecoilTorqueMin, Max)`.

### 3.3 Оружие (конфиги = живая правда; значения на 2026-09-10)
| Параметр | Пистолет | Автомат | Дробовик |
|---|---|---|---|
| Паттерн | Single | Burst | Shotgun |
| DisplayName | Пистолет | Автомат | Дробовик |
| Урон | 1 | 1 | 1 (дробь ×0.34) |
| КД (между тапами) | 0.8 | 1.1 | 1.3 |
| Магазин | 6 | 9 | 4 |
| Реген (1 патрон) | 2.5 с | 2.5 с | 3 с |
| Полная перезарядка | 3 с | 3.2 с | 3.5 с |
| Скорость пули | 13 | 14 | 12 |
| Радиус пули | 0.15 | 0.12 | 0.1 |
| Отдача | 2 | 1.4 | 3.5 |
| Торк | 30–90° | 40–110° | 60–160° |
| Прочее | — | очередь 3, интервал 0.12 с | 6 дробин, веер 30–32° |

- **Single**: тап = 1 пуля.
- **Burst**: тап списывает 1 патрон и стреляет, затем очередь добивает
  оставшиеся (BurstCount − 1) с интервалом 0.12 с; тап во время очереди
  игнорируется; шоты очереди игнорируют оружие-КД (ставят КД = интервал
  очереди); пустой магазин в очереди → полная перезарядка, очередь
  сбрасывается.
- **Shotgun**: тап = 1 патрон = PelletCount дробин веером SpreadAngle
  вокруг направления ствола, урон дробины = Damage × PelletDamageMultiplier,
  отдача и звук — один раз.
- Расход/состояния (общие): CanFire = не перезаряжается И магазин > 0
  И КД <= 0; выстрел списывает патрон и ставит КД; магазин 0 → полная
  перезарядка ReloadRemaining = FullReloadSeconds; реген: +1 патрон каждые
  RegenDelay, только вне перезарядки; пуля спавнится от дула:
  `muzzle = позиция + направление · (радиус пушки + радиус пули +
  BulletSpawnOffsetUnits 0.05)`.

### 3.4 Пули и взрывы
- Пуля: позиция, скорость, радиус, урон, владелец. Движение по прямой.
- Коллизии — руками (без физдвижка!): swept «отрезок за тик vs круг»
  против стен-прямоугольников (границы арены + радиус пули), круговых
  препятствий (радиус + радиус пули), пушек (только ЧУЖИЕ пушки; своя
  пуля владельца не бьёт), и пуля×пуля (отрезок одной vs круг другой,
  симметрично).
- Смерть пули: стена / препятствие / пушка (урон) / взрыв.
- Взрыв (пуля×пуля): позиция = середина между пулями; убивает обе пули;
  ПРОВЕРКА IsAlive пары (мёртвая пуля не цепляет новые взрывы в том же
  тике — иначе двойной урон кластера); урон каждой пушке, чей центр в
  `RadiusUnits + радиус пушки` (ExplosionConfig: радиус 0.85–1.0, урон 1).
  После конца матча события взрывов/попаданий не переиздаются.

### 3.5 ИИ врага (один канал стрельбы)
- Рандомный КД: `Random(FireCooldownMin 1.6, Max 2.6)`; истёк → выстрел в
  текущем направлении ствола. Доводки на игрока НЕТ.
- Конус зрения 100° вокруг направления ствола + LOS (отрезок не пересекает
  препятствия). Проверка каждый тик.
- «Появление» игрока: вход в зону после ≥ VisibilityDebounceSeconds (0.3 с)
  непрерывного отсутствия. Появление срезает текущий КД до ReactionSeconds
  (0.2), ЕСЛИ с последнего выстрела прошло ≥ ReactionSeconds. Старт боя
  «появлением» не считается (игрок виден с спавна). Смена стадии врага
  гистерезис НЕ сбрасывает (только новый КД стадии).
- Враг не имеет патронов (бесконечные), использует паттерн своего оружия
  (очередь/дробь через тот же FireSystem), отдача работает полностью.

### 3.6 Урон и стадии
- Игрок: MaxHealth (живая правда в ассете), i-frames InvulnerabilitySeconds
  = 0.6 с от начала Fight (по таймеру боя). Враг: 3 стадии по StageHealths,
  остаток урона переносится в следующую стадию; смена стадии = событие
  (цвет/звук). Победа: последняя стадия ≤ 0. Поражение: HP игрока ≤ 0.
  Одновременная смерть = Victory (проверка врага раньше).

### 3.7 Экономика и арсенал
- Награда за победу: `BaseReward + RewardPerLevel × (уровень − 1)`
  (ассет: BaseReward 25, RewardPerLevel 1), начисляется в деньги.
- Деньги/уровень живут на сессию до появления сейвов (М3), потом —
  YG player data.
- Арсенал (магазин): 3 карточки оружия, клик = экипировать, «В БОЙ»
  стартует матч. Экипированное оружие игрока; врагов это не касается.

### 3.8 Парирование (Ф4 — следующий контент после миграции)
Канон «нажал = выстрел»: 1) тап стреляет как обычно; 2) параллельно
открывается окно ParryWindowSeconds (0.2); 3) пока окно открыто, ищем
вражеские пули в ParryRadius от игрока; 4) попала — пуля становится нашей
(IsParried), летит ОТ игрока В СТОРОНУ СТРЕЛКА со скоростью ×
ParriedBulletSpeedMultiplier, RicochetsLeft = 2–3 (рикошеты от стен/препятствий
не убивают пулю, пока счётчик > 0); 5) окно закрывается по таймеру ИЛИ
успешному парри; анти-дабл-парри КД 0.1 с. Игроковые конфиг-поля:
ParryWindowSeconds / ParryRadius / ParryCooldownSeconds /
ParriedBulletSpeedMultiplier / ParriedBulletRicochets.

### 3.9 Баффы (Ф6), контент после Ф4/Ф5
Капсулы дрейфуют по арене, подбор касанием игрока. TimeSlow (замедление
ТОЛЬКО врага и его пуль — не глобальный timescale!), FastReload, ArenaStrike
(урон 1.0 только врагу сквозь препятствия), Minigun (автострельба без
расхода, хаос отдачи). Спавн по интервалу, лимит одновременных.

### 3.10 Паттерны/траектории (Ф5, бэклог)
Оставшиеся паттерны: Auto (удержание). Траектории: Sine, Homing (руль к
вражеской пушке с HomingTurnRate), Accelerating. Скалирование от уровня:
множители КД/HP, элита каждые 10 уровней (HP ×2).

### 3.11 Мета (Ф7) и платформа (Ф8)
- Перки: 5 шт (КД/магазин/реген/урон/отдача), ранги до 5, цены растут,
  кап эффектов; единственная точка слияния `Apply(weaponParams)`.
- Инвентарь: купленные оружия/скины, EquippedWeaponId.
- Скины: каталог + экипировка ID (визуал = цветовые палитры пушки).
- Сейвы: YG player data: версия, деньги, уровень, купленные оружия
  (List<string>), экипированный ID, ранги перков, язык, звук. Каданс:
  на старте дуэли + после покупок; троттлинг записи.
- Реклама: interstitial на переходах уровней (НЕ между Boot→Game), гейт
  60 с; rewarded — кнопка «Возродиться» (кап 1/бой): восстановить HP/
  патроны + N с быстрой перезарядки.
- Локализация: ru/en/tr (автоперевод вычитать вручную), настройки языка
  и звука в сейве.
- Модерация ЯИ: GameplayStart/Stop обязателен, GameReady после init,
  ноль внешних ссылок, звук по умолчанию вкл с кнопкой mute.

### 3.12 После релиза
Лидерборд (макс. уровень), sticky-баннеры, Payments (инапы скинов),
обучение парри.

---

## 4. АРХИТЕКТУРА PHASER-ПРОЕКТА

### 4.1 Стек и каркас
- TypeScript `strict` (запрещён `any`, `!` только по обоснованию),
  Vite, Phaser 3 (последняя стабильная 3.8x), vitest для тестов симуляции,
  ESLint (правила из раздела 7). Цель сборки — статические файлы в
  `/dist` (index.html + js + всё инлайн-загружаемое), бюджет размера
  **≤ 5 МБ** (реально выйдет ~1–2 МБ).
- Ноль внешних asset-файлов: графика и звук — процедурные (см. 4.5/4.6).
  Это требование владельца (проверено: работает и выглядит как игра).
- Репозиторий: НОВЫЙ git-репозиторий `pistol-panic-phaser` (этот репозиторий
  остаётся архивом Unity-версии). Один коммит = один шаг фазы.

### 4.2 Главный архитектурный принцип (переносится 1:1 из Unity-версии)
**Детерминированная симуляция, отделённая от отображения.**
- `src/sim/` — чистый TypeScript БЕЗ импортов Phaser. Фиксированный шаг
  1/60 c через аккумулятор (порт SimulationDriver: unscaledDelta × timeScale,
  max 3 шага/кадр, clamp остатка вместо сброса). Вся боевая математика —
  здесь. Ноль аллокаций в тике: пул пуль (массив + freelist, пре-варм 64),
  числовые поля вместо объектов-снапшотов там, где горячо.
- `src/view/` — Phaser-вьюхи. Симуляция НЕ знает о Phaser. Вьюхи подписаны
  на события симуляции (типизированный emitter) и рисуют. Отписка — в
  destroy.
- `src/yg/` — обёртка Yandex SDK с фейком для локальной разработки
  (`YgMock`: эмулирует init/сейвы/рекламу, чтобы игра запускалась с
  `vite dev` без платформы).
- `src/audio/` — WebAudio-синтез (порт ProceduralSoundFactory: выстрелы,
  попадание, взрыв, стадия, перезарядка, победа/поражение, UI-клик;
  громкости из конфига; разблокировка аудиоконтекста по первому тапу —
  политика автоплея!).
- `src/art/` — процедурная графика: генерация текстур в canvas-тайпы Phaser
  (порт PixelArtFactory: пиксель-арт стволы 48×24 с обводкой/затенением,
  трассеры, звезда вспышки, кольцо взрыва, бевел-стены, объёмные
  препятствия, градиентный пол, 9-slice панели). Палитры: PlayerSteel /
  EnemyRed / EnemyOrange / EnemyPurple (арт-референс — ветка
  `unity-pixelart-wip`, файлы PixelArtFactory.cs и SpriteFactory.cs).
- `src/config/` — конфиги-объекты (порт SO-ассетов, значения из таблицы 3.3
  и раздела 3): weaponConfigs (pistol/automat/shotgun), enemyCatalog
  (3 типа: оружие + GunSkin + цвета стадий), physics, map, player, enemy AI,
  explosion, economy, simulation (60 тиков/3 шага/60 fps), audio.
- Точка входа: `src/main.ts` — state machine приложения:
  `boot (ждёт YG init) → duel → result → shop → duel …`. Никаких сцен Unity —
  один Phaser.Scene + явные состояния.

### 4.3 Карта переноса C# → TypeScript
| Unity (Assets/Scripts) | Phaser (src/) | Примечание |
|---|---|---|
| SimulationDriver | sim/SimulationDriver.ts | аккумулятор, clamp backlog |
| DuelSim | sim/DuelSim.ts | оркестратор + публикация событий |
| DuelFlow | sim/DuelFlow.ts | FSM + fight-clock |
| MovementSystem | sim/MovementSystem.ts | |
| AmmoSystem/AmmoState | sim/AmmoSystem.ts | |
| FireSystem | sim/FireSystem.ts | паттерны Single/Burst/Shotgun, очереди |
| BulletSystem/BulletState | sim/BulletSystem.ts | пул + swept-коллизии |
| CollisionMath | sim/CollisionMath.ts | отрезок-круг, конус, круг-круг |
| ExplosionSystem | sim/ExplosionSystem.ts | правило «домажит кто в радиусе» |
| DamageSystem | sim/DamageSystem.ts | i-frames по fight-clock, стадии |
| AiShooterSystem | sim/AiShooterSystem.ts | гистерезис, без сброса на стадии |
| Конфиги-SO | config/*.ts | значения из ассетов (раздел 3.3) |
| HudView, DuelResultOverlayView, ShopWeaponsView, ViewBinder, ArenaView, GunView, BulletPool, FxPool, FlashFxView | view/*.ts | Phaser-объекты, подписки |
| ProceduralSoundFactory/AudioService | audio/*.ts | WebAudio вместо AudioClip |
| PixelArtFactory/SpriteFactory | art/*.ts | canvas-текстуры Phaser |
| WeaponLoadoutService/Providers | sim/loadout.ts | выбор оружия игрока/врага |
| MatchResultApplier | sim/matchFlow.ts | награда/уровень/деньги |
| YgLifecycleAdapter | yg/YgSdk.ts + YgMock.ts | см. раздел 5 |
| SceneLoader/сцены | state machine | нет аналога, не нужен |

Порядок порта строго: конфиги → CollisionMath → MovementSystem →
AmmoSystem → BulletSystem → ExplosionSystem → DamageSystem →
AiShooterSystem → DuelFlow → FireSystem → DuelSim. Симуляция покрывается
vitest-тестами ДО написания вьюхов (см. раздел 8) — это главная страховка
миграции: C#-версия в этом репо — эталон поведения.

### 4.4 События (порт DuelSim-событий)
Типизированный emitter: `playerGunUpdated / enemyGunUpdated /
bulletSpawned / bulletMoved / bulletRemoved / explosionHappened / gunHit /
phaseChanged / enemyStageChanged / shotFired (snapshot: позиция, чей,
паттерн) / playerAmmoChanged (магазин, размер, прогресс регена,
флаг+прогресс перезарядки) / matchFinished (phase, reward)`.
Снапшоты — плоские интерфейсы. Публиковать ammo только при изменении
значений (как в Unity-версии).

---

## 5. YANDEX GAMES SDK

- Пакет `@yandex-games/sdk-dev` (или ручная загрузка `sdk.js` с ЯИ-хоста в
  dev-заглушке). Вся работа через `src/yg/YgSdk.ts` — единственное место,
  знающее о SDK.
- Поток: `YaGames.init()` → `ysdk.features.LoadingAPI.ready()` (= MarkReady)
  → `gameplayStart()/gameplayStop()` на Armed-паузах и оверлеях
  (требование модерации) → `player.setData/getData` для сейвов →
  `adv.showFullscreenAdv` (гейт 60 с, на переходах уровней) /
  `adv.showRewardedVideo` (возрождение) → `getLanguage()` (ru/en/tr) →
  leaderboards `setLeaderboardScore('maxLevel', …)` → payments (после
  релиза, скины).
- Обёртка возвращает Promise; ТАЙМАУТ init 10 с с громкой ошибкой (в
  Unity-версии тот же подход). `YgMock` повторяет интерфейс 1:1 для
  локального dev.
- Перед релизом: чеклист модерации ЯИ — ready-вызов, gameplay-события,
  никакой выдачи наружу, работа без интернета (сейвы в кэш), баннеры
  не перекрывают геймплей, релизная сборка без дев-логов.

---

## 6. ПЛАН РАБОТ (фазы миграции; один шаг = один коммит)

- **M0. Каркас**: репо, Vite+TS+ESLint+vitest, Phaser boot, state machine,
  конфиги-объекты, YgMock. DoD: пустая арена рисуется, `tsc` чист, бандл
  ≤ 5 МБ.
- **M1. Симуляция боя 1:1**: порт по карте 4.3 (движение, стрельба всех
  трёх паттернов, пули+swept, взрывы, урон/стадии, ИИ с гистерезисом,
  3 типа врагов по уровню). DoD: vitest-набор воспроизводит эталонные
  сценарии (кейсы ниже в 8.2), дуэль играется консольно без графики.
- **M2. Отображение и звук**: процедурный пиксель-арт (арт-референс —
  ветка `unity-pixelart-wip`), HUD (уровень/деньги/патроны/стадии/фаза),
  оверлей результата, FX (вспышка/попадание/взрыв), WebAudio-синтез всех
  звуков, экран АРСЕНАЛ. DoD: игра от тапа до магазина выглядит и звучит
  как готовая casual-игра на мобиле.
- **M3. Yandex SDK**: init/ready/gameplay, сейвы (порт формата из 3.11),
  rewarded-возрождение, interstitial-гейт, язык. DoD: на черновике ЯИ
  сейв переживает перезагрузку, реклама показывается по гейту.
- **M4. Контент Ф4–Ф6**: парирование (канон 3.8) + рикошеты, оставшиеся
  траектории/паттерны и скалирование/элита, баффы. DoD: чекпоинты фаз из
  PHASES_README (разделы C/D/E) выполняются.
- **M5. Мета Ф7**: перки, инвентарь, скины (палитры), полноценный магазин,
  возрождение за рекламу. DoD: полный цикл «дуэль → деньги → прокачка →
  дуэль», сейв после покупок.
- **M6. Релиз Ф8**: локализация ru/en/tr, настройки, лидерборд, сборка,
  модерация ЯИ. DoD: билд прошёл модерацию, метрики загрузки в норме.

После релиза (из ГДД): лидерборд-расширения, sticky-баннеры, Payments
(скины), обучение парри.

---

## 7. ПРАВИЛА КОДА (порт AI_RULES.md на TypeScript; читать и оригинал)

- `strict: true`, без `any`, без `!` без причины; `const` везде, где можно.
- Запрещено: комментарии (никогда), `any`, `enum`-строки-магия, статические
  глобальные состояния, Service Locator, классы с именами Manager/Helper,
  обобщённые «на будущее» интерфейсы, вложенные тернарники, неявные
  мутации параметров.
- Именование: приватные поля `_camelCase`, типы/методы `PascalCase`,
  булевы `isSomething/wasSomething`, конфиги `*Config`.
- Формат: скобка на новой строке, тело — всегда многострочный блок,
  ранние return'ы, поток сверху вниз, пустые строки между блоками.
- Один файл = один класс/модуль одной ответственности.
- Зависимости — через конструктор; в Phaser-вьюхах — через инициализацию
  сценой. Никаких синглтонов-глобалок, кроме единственной точки входа.
- Симуляция: никаких обращений к Phaser/DOM; никакого `Math.random()` в
  симуляции кроме мест, где рандом задуман (КД ИИ, торк, спавн-импульс) —
  там инжектить `random: () => number` в конструктор симуляции для
  тестируемости.
- Подписки: только именованные методы-хендлеры; отписка обязательна.
- Баланс — только в `src/config`, ноль чисел в логике (кроме
  структурных: деление на dt, индексы).
- ESLint: `@typescript-eslint` recommended + запрет `any` + запрет
  `prefer-const`-нарушений; прогон обязателен перед коммитом.

---

## 8. ВЕРИФИКАЦИЯ (чем агент доказывает, что порт верен)

### 8.1 Автоматика
- `tsc --noEmit` + ESLint + vitest зелёные перед каждым коммитом.
- Бюджет бандла: `vite build` ≤ 5 МБ (проверять скриптом, падать при
  превышении).
- Headless-smoke: vitest+jsdom или puppeteer-скрипт, который грузит билд,
  дожидается `ready`, эмулирует тап, проверяет, что duelSim жив и сцена
  Game отрисована.

### 8.2 Эталонные тесты симуляции (обязательный набор)
1. Пушка со спавн-импульсом останавливается дампингом и не вылетает за
   стены (позиция зажата радиусом).
2. Выстрел Single: −1 патрон, КД активен, пуля на дуле с оффсетом,
   скорость = направление × BulletSpeed.
3. Burst: ровно 3 пули с интервалом ~0.12 с, тап во время очереди
   игнорируется, пустой магазин обрывает очередь и стартует перезарядку.
4. Shotgun: 6 пуль, углы от −16° до +16°, урон каждой ×0.34, один звук.
5. Пуля бьёт только чужую пушку; попадание в стену/препятствие убивает.
6. Взрыв: урон тому, кто в радиусе+радиус пушки; мёртвая пуля не даёт
   цепной взрыв в том же тике; после победы события не дублируются.
7. ИИ: старт боя не даёт мгновенного выстрела при видимом игроке;
   смена стадии не срезает КД «появлением»; появление после 0.3 с
   невидимости срезает КД до 0.2 с.
8. Победа на последней стадии с переносом остатка урона; поражение при
   HP ≤ 0; i-frames 0.6 с.
9. Деньги: награда за победу по формуле, +уровень за любой исход,
   тип врага циклится (1,2,3,1…).

### 8.3 Ручной чек (владелец)
Мобила/DevTools-throttle: загрузка < 3 с, стабильные 60 fps, звуки после
первого тапа, реклама по гейту, сейв перезагрузки.

---

## 9. РИСКИ И ПЛАН ОТКАТА

- WebAudio на iOS/Safari: разблокировка первого тапом, иначе mute. Тест
  на реальном iPhone обязателен в М2.
- Phaser 3 vs 4: брать стабильную 3.8x; на 4 переходить не стоит.
- Если M1–M2 не сходятся по поведению с эталоном за разумное время —
  Unity-версия (`cc38ddc`) остаётся рабочим фолбэком, ничего не теряется.
- Не тащить из Unity лишнее: сцены/скоупы/asmdef/VContainer/UniTask/
  PluginYG2 — умерли, их замены описаны выше.

---

## 10. СТАТУС Unity-ВЕРСИИ (справка)

- Рабочий коммит: `cc38ddc` (ветка `otecGlinomes83/PistolPanic`), проверен
  headless-компиляцией и запуском.
- `unity-pixelart-wip` — незавершённый пиксель-арт (компиляцию не проходил),
  использовать ТОЛЬКО как арт-референс (PixelArtFactory.cs — палитры и
  формы спрайтов).
- Инструмент проверки Unity-версии (если понадобится):
  `unity run <path> --editor-version 2022.3.62f2 -- -nographics
  -executeMethod PistolPanic.EditorTools.ProjectValidator.ValidateAndReport`.
